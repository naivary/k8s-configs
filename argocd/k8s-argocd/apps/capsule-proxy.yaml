---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: capsule-proxy
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
  - repoURL: https://projectcapsule.github.io/charts
    chart: "capsule-proxy"
    targetRevision: 0.7.1
    helm:
      valueFiles:
      - $values/capsule-proxy/values.yaml
      ignoreMissingValueFiles: true
  - repoURL: https://github.com/naivary/k8s-argocd
    targetRevision: HEAD
    ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: capsule-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
