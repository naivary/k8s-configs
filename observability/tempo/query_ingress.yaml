apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tempo-query
  namespace: monitoring
spec:
  ingressClassName: "nginx"
  rules:
  - http:
     paths:
     - path: /
       pathType: Prefix
       backend:
         service:
           name: tempo-query-frontend
           port:
             name: http-metrics
